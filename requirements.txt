# Library Management System - Python Dependencies
# Class XII Computer Science Project

# Required for running the application
mysql-connector-python>=8.0.0
Pillow>=9.0.0

# Required for building Windows executable (optional)
pyinstaller>=5.0.0

# Required for converting documentation to Word format (optional)
python-docx>=1.2.0

# Python standard library modules (already included with Python):
# - tkinter (GUI framework)
# - datetime (date/time handling)
# No additional installation needed for standard library modules
